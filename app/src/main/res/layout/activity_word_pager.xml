<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar_pager"
        android:layout_width="match_parent"
        android:background="@color/toolbar_background_color"
        android:layout_height="50dp"
        android:theme="@style/ToolbarTheme" />

    <android.support.v4.widget.DrawerLayout
        android:id="@+id/activity_word_pager_drawer_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/toolbar_pager">

        <android.support.v4.view.ViewPager
            android:id="@+id/activity_word_pager_view_pager"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/toolbar_pager" />

        <android.support.design.widget.CoordinatorLayout
            android:id="@+id/snackbarPosition"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:foregroundGravity="center"/>

        <android.support.v7.widget.RecyclerView
            android:id="@+id/activity_word_pager_nav_list"
            android:layout_width="100dp"
            android:layout_height="match_parent"
            android:layout_gravity="right"
            android:background="@color/general_background_color"
            android:scrollbars="vertical" />
    </android.support.v4.widget.DrawerLayout>


</RelativeLayout>
